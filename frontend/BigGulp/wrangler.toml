name = "big-gulp"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Set VITE_API_URL and VITE_API_KEY in the Cloudflare Pages dashboard
# under Settings → Environment variables (Production + Preview).
# Do NOT put real values here — this file is not secret.

[env.production.vars]
# VITE_API_URL = "https://your-railway-url.railway.app"
# (API key goes in the dashboard as a Secret, not here)
